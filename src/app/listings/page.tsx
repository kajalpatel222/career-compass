import { JobListings } from "@/app/_components/job-listings";

export default function ListingsPage() {
  return <JobListings allowSessionFallback description="All job boards, with the strongest matches first." eyebrow="" title="Listings" />;
}
