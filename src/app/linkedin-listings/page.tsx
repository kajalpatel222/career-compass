import { JobListings } from "@/app/_components/job-listings";

export default function LinkedInListingsPage() {
  return <JobListings description="Paid LinkedIn results are kept here after each daily search." eyebrow="" source="LinkedIn" title="LinkedIn listings" />;
}
